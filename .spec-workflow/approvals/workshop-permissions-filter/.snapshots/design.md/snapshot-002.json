{
  "id": "snapshot_1760336308094_vcffx4k0g",
  "approvalId": "approval_1760335782953_xu0p9pyjn",
  "approvalTitle": "车间权限过滤 - 设计文档",
  "version": 2,
  "timestamp": "2025-10-13T06:18:28.094Z",
  "trigger": "revision_requested",
  "status": "pending",
  "content": "# Design Document\n\n## Overview\n\n本设计实现基于用户 `pagePermissions` 字段的车间数据过滤功能，应用于月度能耗统计和日能耗统计两个API。通过在Service层解析用户权限并在Mapper层使用动态SQL过滤，实现数据级访问控制。\n\n**核心设计理念**：\n- 在Service层解析 `pagePermissions` 字段，生成车间名称列表\n- 将车间列表传递给Mapper层\n- 在SQL层面使用 `IN` 子句过滤数据，提高查询效率\n- 管理员自动拥有全部权限，普通用户按权限过滤\n\n## Steering Document Alignment\n\n### Technical Standards (tech.md)\n\n根据 `structure.md` 中的技术栈：\n- **后端框架**: Spring Boot 2.7.2 + MyBatis-Plus 3.5.2\n- **数据库**: SQL Server (能耗数据)\n- **日志**: SLF4J + Lombok `@Slf4j`\n- **JSON解析**: Gson（已在项目中使用）\n\n本设计遵循以下技术标准：\n- 使用MyBatis动态SQL（`<choose>`, `<foreach>`）\n- 使用Lombok简化代码\n- 使用INFO/WARN级别日志记录关键操作\n- 保持现有Service层的代码风格\n\n### Project Structure (structure.md)\n\n**改动文件位置**：\n```\nback2/src/main/java/com/yupi/springbootinit/\n├── service/impl/\n│   ├── MonthlyEnergyServiceImpl.java    ✏️ 修改\n│   └── HourlyEnergyServiceImpl.java     ✏️ 修改\n├── mapper/sqlserver/\n│   ├── MonthlyEnergyMapper.java         ✏️ 修改\n│   └── HourlyEnergyMapper.java          ✏️ 修改\n\nback2/src/main/resources/mapper/sqlserver/\n├── MonthlyEnergyMapper.xml              ✏️ 修改\n└── HourlyEnergyMapper.xml               ✏️ 修改\n```\n\n## Code Reuse Analysis\n\n### Existing Components to Leverage\n\n- **`LoginUserVO`**: 当前登录用户对象，包含 `pagePermissions` 字段和 `userRole` 字段\n- **`EnergyCalculationUtils`**: 能耗计算工具类，无需修改，继续复用\n- **`WORKSHOP_ORDER`**: 两个Service类中已定义的车间顺序常量，可直接复用作为映射基础\n\n### Integration Points\n\n- **用户Session**: 从 `request.getSession()` 或Spring Security Context获取当前登录用户\n- **Gson**: 使用项目中已有的Gson依赖解析JSON格式的 `pagePermissions`\n- **MyBatis动态SQL**: 利用MyBatis的 `<choose>` 和 `<foreach>` 标签实现条件查询\n\n## Architecture\n\n### 整体流程\n\n```mermaid\nsequenceDiagram\n    participant C as Controller\n    participant S as Service层\n    participant M as Mapper层\n    participant DB as SQL Server\n\n    C->>S: 调用getMonthlyStatistics(year, month)\n    S->>S: 1. 获取当前登录用户\n    S->>S: 2. 检查userRole是否为admin\n    alt 管理员\n        S->>S: workshopList = null (查所有)\n    else 普通用户\n        S->>S: 3. 解析pagePermissions (JSON)\n        S->>S: 4. 映射路径→车间名称\n        S->>S: workshopList = [\"114_空调水机主机\", \"110注射\"]\n    end\n    S->>M: selectMonthlyRawData(workshopList, startTime, endTime)\n    M->>M: 5. 构建动态SQL\n    alt workshopList != null && size > 0\n        M->>DB: WHERE Workshop IN ('114_空调水机主机', '110注射')\n    else workshopList != null && size == 0\n        M->>DB: WHERE 1=0 (返回空)\n    else workshopList == null\n        M->>DB: 无Workshop过滤条件 (查所有)\n    end\n    DB-->>M: 返回数据\n    M-->>S: List<TempMonitor>\n    S->>S: 6. 按车间分组计算能耗\n    S->>S: 7. 仅保留workshopList中的车间\n    S-->>C: MonthlyEnergyStatistics\n```\n\n### Modular Design Principles\n\n- **单一职责原则**: \n  - Service层负责权限解析和业务逻辑\n  - Mapper层只负责数据查询\n- **最小改动**: 只修改现有的6个文件，不引入新类\n- **向后兼容**: API响应结构不变，只改变返回的车间数量\n\n## Components and Interfaces\n\n### Component 1: MonthlyEnergyServiceImpl (Service层)\n\n**Purpose**: 解析用户权限，生成车间列表，调用Mapper查询数据\n\n**新增逻辑**:\n```java\n// 1. 获取当前登录用户\nUser currentUser = userService.getLoginUser(request);\n\n// 2. 检查是否为管理员\nList<String> allowedWorkshops = null;\nif (!\"admin\".equals(currentUser.getUserRole())) {\n    // 3. 解析pagePermissions\n    allowedWorkshops = parseWorkshopsFromPermissions(currentUser.getPagePermissions());\n}\n\n// 4. 调用Mapper（传入车间列表）\nList<TempMonitor> allRawData = monthlyEnergyMapper.selectMonthlyRawData(\n    allowedWorkshops, monthStart, monthEnd);\n```\n\n**核心方法**:\n```java\n/**\n * 从pagePermissions解析用户有权访问的车间列表\n * @param pagePermissions JSON格式的页面权限字符串\n * @return 车间名称列表，如果解析失败返回空列表\n */\nprivate List<String> parseWorkshopsFromPermissions(String pagePermissions) {\n    List<String> allowedWorkshops = new ArrayList<>();\n    \n    // 空权限处理\n    if (pagePermissions == null || pagePermissions.trim().isEmpty() || \"[]\".equals(pagePermissions)) {\n        log.warn(\"用户pagePermissions为空，将返回空车间列表\");\n        return allowedWorkshops;\n    }\n    \n    try {\n        // 使用Gson解析JSON数组\n        Gson gson = new Gson();\n        String[] paths = gson.fromJson(pagePermissions, String[].class);\n        \n        // 映射路径到车间名称\n        Map<String, String> pathToWorkshop = buildPathToWorkshopMapping();\n        \n        for (String path : paths) {\n            if (pathToWorkshop.containsKey(path)) {\n                allowedWorkshops.add(pathToWorkshop.get(path));\n            }\n        }\n        \n        log.info(\"解析到{}个有权访问的车间: {}\", allowedWorkshops.size(), allowedWorkshops);\n        \n    } catch (Exception e) {\n        log.warn(\"解析pagePermissions失败: {}, 将返回空车间列表\", e.getMessage());\n    }\n    \n    return allowedWorkshops;\n}\n```\n\n**Dependencies**: \n- `UserService` (获取当前登录用户)\n- `Gson` (JSON解析)\n- `MonthlyEnergyMapper` (数据查询)\n\n**Reuses**: 现有的 `WORKSHOP_ORDER` 常量\n\n---\n\n### Component 2: HourlyEnergyServiceImpl (Service层)\n\n**Purpose**: 与月度统计相同，解析权限并查询日能耗数据\n\n**新增逻辑**: 与 `MonthlyEnergyServiceImpl` 完全一致\n\n**核心方法**: 复用 `parseWorkshopsFromPermissions()` 方法（建议提取为工具方法或抽象父类）\n\n---\n\n### Component 3: MonthlyEnergyMapper (Mapper接口层)\n\n**Purpose**: 定义查询方法签名\n\n**修改前**:\n```java\nList<TempMonitor> selectMonthlyRawData(@Param(\"workshop\") String workshop,\n                                       @Param(\"startTime\") Date startTime,\n                                       @Param(\"endTime\") Date endTime);\n```\n\n**修改后**:\n```java\nList<TempMonitor> selectMonthlyRawData(@Param(\"workshopList\") List<String> workshopList,\n                                       @Param(\"startTime\") Date startTime,\n                                       @Param(\"endTime\") Date endTime);\n```\n\n**变更说明**: \n- 移除 `workshop` 单个车间参数\n- 新增 `workshopList` 车间列表参数（可为null或空列表）\n\n---\n\n### Component 4: MonthlyEnergyMapper.xml (Mapper SQL层)\n\n**Purpose**: 使用动态SQL根据车间列表过滤数据\n\n**修改后的SQL**:\n```xml\n<select id=\"selectMonthlyRawData\" resultMap=\"TempMonitorResultMap\">\n    SELECT \n        Id, DeviceID, Name, Tem, Hum, MAC,\n        UpdateTime, ElectricEnergy, NodeID, Workshop\n    FROM RSWS_TempMonitor_Copy\n    WHERE ElectricEnergy IS NOT NULL\n      AND Workshop != '备用'\n      AND Workshop != '备用总表'\n      <choose>\n          <!-- 情况1: 有权限车间列表（普通用户有权限） -->\n          <when test=\"workshopList != null and workshopList.size() > 0\">\n              AND Workshop IN\n              <foreach collection=\"workshopList\" item=\"workshop\" \n                       open=\"(\" separator=\",\" close=\")\">\n                  #{workshop}\n              </foreach>\n          </when>\n          <!-- 情况2: 空权限列表（普通用户无权限） -->\n          <when test=\"workshopList != null and workshopList.size() == 0\">\n              AND 1=0\n          </when>\n          <!-- 情况3: workshopList == null（管理员，查所有车间）-->\n          <!-- 不添加额外条件 -->\n      </choose>\n      <if test=\"startTime != null\">\n          AND UpdateTime &gt;= #{startTime}\n      </if>\n      <if test=\"endTime != null\">\n          AND UpdateTime &lt;= #{endTime}\n      </if>\n    ORDER BY Workshop, Name, UpdateTime ASC\n</select>\n```\n\n**动态SQL逻辑**:\n- `workshopList != null && size > 0`: 使用 `IN` 子句过滤指定车间\n- `workshopList != null && size == 0`: 使用 `WHERE 1=0` 返回空结果\n- `workshopList == null`: 不添加Workshop过滤条件，查询所有车间\n\n---\n\n### Component 5: HourlyEnergyMapper & HourlyEnergyMapper.xml\n\n**Purpose**: 与月度Mapper完全一致的修改\n\n**接口修改**:\n```java\nList<TempMonitor> selectAllWorkshopsHourlyData(\n    @Param(\"workshopList\") List<String> workshopList,  // 新增\n    @Param(\"startTime\") Date startTime,\n    @Param(\"endTime\") Date endTime);\n```\n\n**XML修改**: 与 `MonthlyEnergyMapper.xml` 相同的动态SQL逻辑\n\n## Data Models\n\n### 输入数据模型\n\n**User实体 (已存在)**:\n```java\npublic class User {\n    private Long id;\n    private String userAccount;\n    private String userRole;          // \"admin\" 或 \"user\"\n    private String pagePermissions;   // JSON格式: [\"\\/airConditioning\", \"\\/injection-110\"]\n    // ... 其他字段\n}\n```\n\n**pagePermissions格式**:\n```json\n[\n  \"/monthly-energy\",\n  \"/hourly-energy\",\n  \"/airConditioning\",\n  \"/injection-110\",\n  \"/granule102\"\n]\n```\n\n### 映射配置数据模型\n\n**路径到车间名称的映射表** (在Service方法中定义):\n```java\nprivate Map<String, String> buildPathToWorkshopMapping() {\n    Map<String, String> mapping = new HashMap<>();\n    mapping.put(\"/airConditioning\", \"114_空调水机主机\");\n    mapping.put(\"/injection_workshop\", \"110注射环保设备\");\n    mapping.put(\"/granulation_workshop\", \"102造粒环保设备\");\n    mapping.put(\"/office_building\", \"1#办公楼\");\n    mapping.put(\"/feeding_workshop\", \"101配料\");\n    mapping.put(\"/granule102\", \"102造粒\");\n    mapping.put(\"/cold-press-103\", \"103冷压\");\n    mapping.put(\"/restoration-104\", \"104还原\");\n    mapping.put(\"/sintering-105\", \"105烧结\");\n    mapping.put(\"/cleaning-106\", \"106清洗\");\n    mapping.put(\"/beading-107\", \"107串珠\");\n    mapping.put(\"/rubber-109\", \"109炼胶\");\n    mapping.put(\"/injection-110\", \"110注射\");\n    mapping.put(\"/edging-111\", \"111开刃\");\n    mapping.put(\"/final-inspection-112\", \"112终检\");\n    mapping.put(\"/warehouse-113\", \"113仓库\");\n    mapping.put(\"/elevator-114\", \"114_2#厂房电梯\");\n    mapping.put(\"/office-area-114\", \"114_2#楼办公区域\");\n    mapping.put(\"/conference-room-114\", \"114_2#楼会议室\");\n    mapping.put(\"/laboratory-114\", \"114_2#楼实验室\");\n    mapping.put(\"/public-114\", \"114公共\");\n    mapping.put(\"/air-compressor-114\", \"114空压机\");\n    mapping.put(\"/charging-pile\", \"充电桩\");\n    mapping.put(\"/tool-rd-center\", \"工具研发中心\");\n    mapping.put(\"/guard-room\", \"门卫室\");\n    mapping.put(\"/canteen\", \"食堂\");\n    mapping.put(\"/dormitory\", \"宿舍楼\");\n    return mapping;\n}\n```\n\n**总计**: 27个车间映射关系\n\n### 输出数据模型\n\n**MonthlyEnergyStatistics (无变更)**:\n```java\n{\n  \"year\": 2025,\n  \"month\": 10,\n  \"daysInMonth\": 31,\n  \"workshopList\": [\"114_空调水机主机\", \"110注射\"],  // 仅包含有权限的车间\n  \"workshopDailyData\": {\n    \"114_空调水机主机\": [120.5, 135.2, ...],      // 仅包含有权限的车间\n    \"110注射\": [200.1, 215.3, ...]\n  },\n  \"dailyTotal\": [320.6, 350.5, ...],              // 基于有权限车间的合计\n  \"workshopMonthlyTotal\": {\n    \"114_空调水机主机\": 3500.5,\n    \"110注射\": 6200.3\n  },\n  \"monthlyTotal\": 9700.8                           // 基于有权限车间的总计\n}\n```\n\n## Error Handling\n\n### Error Scenarios\n\n#### 1. pagePermissions解析失败\n\n**场景**: pagePermissions字段包含非法JSON格式\n```java\npagePermissions = \"not a json\"\n```\n\n**处理逻辑**:\n```java\ntry {\n    String[] paths = gson.fromJson(pagePermissions, String[].class);\n} catch (JsonSyntaxException e) {\n    log.warn(\"解析pagePermissions失败: {}, 将返回空车间列表\", e.getMessage());\n    return new ArrayList<>();  // 返回空列表\n}\n```\n\n**用户影响**: \n- API正常返回，但 `workshopList` 为空数组\n- 前端显示\"暂无数据\"\n\n#### 2. 用户无任何车间权限\n\n**场景**: pagePermissions为空或只包含非车间页面\n```json\npagePermissions = \"[]\"\n或\npagePermissions = \"[\"/monthly-energy\", \"/hourly-energy\"]\"\n```\n\n**处理逻辑**:\n- Service层生成空车间列表 `[]`\n- Mapper使用 `WHERE 1=0` 返回空结果\n- Service返回空统计对象\n\n**用户影响**: 前端显示\"暂无数据\"\n\n#### 3. 获取当前用户失败\n\n**场景**: 用户未登录或Session过期\n\n**处理逻辑**:\n```java\nUser currentUser = userService.getLoginUser(request);\nif (currentUser == null) {\n    throw new BusinessException(ErrorCode.NOT_LOGIN_ERROR);\n}\n```\n\n**用户影响**: API返回401错误，前端跳转到登录页\n\n#### 4. 数据库查询失败\n\n**场景**: SQL Server连接失败或查询超时\n\n**处理逻辑**: \n- MyBatis自动抛出 `DataAccessException`\n- 全局异常处理器捕获并返回500错误\n\n**用户影响**: 前端显示\"系统错误，请稍后重试\"\n\n## Testing Strategy\n\n### Unit Testing\n\n**测试Service层的权限解析逻辑**:\n\n```java\n@Test\npublic void testParseWorkshopsFromPermissions_ValidJson() {\n    String permissions = \"[\\\"/airConditioning\\\", \\\"/injection-110\\\"]\";\n    List<String> result = service.parseWorkshopsFromPermissions(permissions);\n    \n    assertEquals(2, result.size());\n    assertTrue(result.contains(\"114_空调水机主机\"));\n    assertTrue(result.contains(\"110注射\"));\n}\n\n@Test\npublic void testParseWorkshopsFromPermissions_EmptyJson() {\n    String permissions = \"[]\";\n    List<String> result = service.parseWorkshopsFromPermissions(permissions);\n    \n    assertEquals(0, result.size());\n}\n\n@Test\npublic void testParseWorkshopsFromPermissions_InvalidJson() {\n    String permissions = \"invalid json\";\n    List<String> result = service.parseWorkshopsFromPermissions(permissions);\n    \n    assertEquals(0, result.size());  // 应返回空列表而非抛出异常\n}\n\n@Test\npublic void testParseWorkshopsFromPermissions_NullPermissions() {\n    String permissions = null;\n    List<String> result = service.parseWorkshopsFromPermissions(permissions);\n    \n    assertEquals(0, result.size());\n}\n```\n\n### Integration Testing\n\n**测试Mapper层的动态SQL**:\n\n```java\n@SpringBootTest\npublic class MonthlyEnergyMapperTest {\n    \n    @Resource\n    private MonthlyEnergyMapper mapper;\n    \n    @Test\n    public void testSelectWithWorkshopList() {\n        List<String> workshops = Arrays.asList(\"114_空调水机主机\", \"110注射\");\n        List<TempMonitor> result = mapper.selectMonthlyRawData(\n            workshops, startTime, endTime);\n        \n        // 验证返回的数据只包含指定车间\n        Set<String> returnedWorkshops = result.stream()\n            .map(TempMonitor::getWorkshop)\n            .collect(Collectors.toSet());\n        assertTrue(returnedWorkshops.size() <= 2);\n    }\n    \n    @Test\n    public void testSelectWithEmptyWorkshopList() {\n        List<String> workshops = new ArrayList<>();\n        List<TempMonitor> result = mapper.selectMonthlyRawData(\n            workshops, startTime, endTime);\n        \n        assertEquals(0, result.size());  // 应返回空结果\n    }\n    \n    @Test\n    public void testSelectWithNullWorkshopList() {\n        List<TempMonitor> result = mapper.selectMonthlyRawData(\n            null, startTime, endTime);\n        \n        assertTrue(result.size() > 0);  // 应返回所有车间数据\n    }\n}\n```\n\n### End-to-End Testing\n\n**测试完整的API流程**:\n\n1. **管理员用户测试**:\n   - 登录管理员账号\n   - 调用 `/api/monthly/statistics?year=2025&month=10`\n   - 验证返回所有27个车间的数据\n\n2. **普通用户有权限测试**:\n   - 登录普通用户（pagePermissions包含2个车间）\n   - 调用月度统计API\n   - 验证 `workshopList` 只包含2个车间\n   - 验证 `workshopDailyData` 只包含这2个车间的数据\n\n3. **普通用户无权限测试**:\n   - 登录普通用户（pagePermissions为空）\n   - 调用月度统计API\n   - 验证 `workshopList` 为空数组\n   - 验证前端显示\"暂无数据\"\n\n4. **异常情况测试**:\n   - 用户的pagePermissions为非法JSON\n   - 验证API不抛出异常，返回空数据\n\n## Performance Considerations\n\n### 优化点\n\n1. **SQL层面过滤**: \n   - 使用 `WHERE Workshop IN (...)` 直接在数据库过滤\n   - 减少网络传输和内存处理的数据量\n\n2. **映射表缓存**:\n   - 将 `buildPathToWorkshopMapping()` 的结果缓存为静态常量\n   - 避免每次请求都重新创建Map\n\n```java\nprivate static final Map<String, String> PATH_TO_WORKSHOP_MAPPING = buildPathToWorkshopMapping();\n```\n\n3. **权限解析优化**:\n   - 考虑将解析后的车间列表缓存在用户Session中\n   - 避免每次请求都解析JSON（可选，视需求而定）\n\n### 性能指标\n\n- **权限解析耗时**: < 10ms (解析JSON + 映射转换)\n- **SQL过滤耗时**: 与现有查询时间相同（利用Workshop索引）\n- **总体响应时间增加**: < 50ms\n\n## Security Considerations\n\n1. **SQL注入防护**: \n   - 使用MyBatis参数绑定 `#{workshop}`\n   - 避免字符串拼接SQL\n\n2. **权限逃逸防护**:\n   - 在Service层和SQL层双重过滤\n   - 即使Service层被绕过，SQL层仍会过滤\n\n3. **日志脱敏**:\n   - 日志中不输出用户敏感信息\n   - 只记录用户ID和车间列表\n\n## Migration and Rollback Plan\n\n### 迁移步骤\n\n1. **阶段1**: 修改Mapper接口和XML（向后兼容）\n2. **阶段2**: 修改Service实现类，添加权限解析逻辑\n3. **阶段3**: 测试验证\n4. **阶段4**: 上线部署\n\n### 回滚方案\n\n如需回滚，只需将6个文件恢复到修改前的版本即可，无需数据库变更。\n\n## Open Questions\n\n1. **是否需要将映射配置外部化？**\n   - 当前方案：硬编码在Service中\n   - 备选方案：配置文件或数据库表\n   - 建议：先硬编码，后续如需频繁调整再考虑外部化\n\n2. **是否需要缓存解析后的车间列表？**\n   - 当前方案：每次请求都解析\n   - 备选方案：缓存在Session或Redis中\n   - 建议：先不缓存，观察性能后决定\n\n3. **pagePermissions中的路径是否需要验证有效性？**\n   - 当前方案：忽略无法映射的路径\n   - 备选方案：记录WARN日志提示无效路径\n   - 建议：记录WARN日志，便于排查配置错误\n",
  "fileStats": {
    "size": 18733,
    "lines": 603,
    "lastModified": "2025-10-13T06:09:36.958Z"
  },
  "comments": []
}